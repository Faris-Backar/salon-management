import 'package:pdf/widgets.dart';

void downloadCSVForWeb({required String csv}) {
  // No-op: Only supported on web
}

Future<void> downloadPdfWeb(Document pdf) async {
  // No-op: Only supported on web
}
